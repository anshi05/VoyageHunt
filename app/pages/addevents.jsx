import { ScrollView } from 'react-native'
import FormUI from '../../components/ui/Form'

const AddEvent = () => {
    return (
        <ScrollView>
            <FormUI />
        </ScrollView>
    )
}

export default AddEvent